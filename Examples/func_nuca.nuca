program PATITO;
a, b : int;
f : float;

void uno(a : int)
VARS {}
{
  a = a + b * a;
  write(a, b, a + b);
}

void dos(a : int, b : int, g : int)
VARS {
  i : int;
}
{
  i = b;
  while (i >   0)
  {
    a = a + b * i + b;
    uno(i * 2);
    write(a);
    i = i - 1;
  }
}

main(){
  a = 3;
  b = a + 1;
  write(a, b);
  f = 3.14;
  dos(a + b * 2, b, 3 * 3);
  write(a, b, f * 2 + 1);
}
