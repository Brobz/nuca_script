program MYPROGRAM;

res : boolean;
a, b, c, d, e, f, g : float;
lol, lol_doubled : str;
another_str  : str;

main () {
  lol = "lol!";
  lol_doubled = lol * 2;
  another_str =  "String manipulation is nice! Look: " + lol_doubled + "This will also be doubled" * 2;
  write(another_str, lol);
  read(a, b, c, d, e, f, g, g);
  res = a * (b + c / d - e) / f * g >  (a *  (b - c) / d) + e;
  write("The result is: ", res)
}
